# Environment variables for the provisioner service (runs on Railway).
# Copy to your Railway service Variables tab and adjust values.
#
# See README → Railway setup for full instructions.

# --- Required ---

# Superuser connection string — reference the Postgres plugin variable.
POSTGRES_URL=${{Postgres.POSTGRES_URL}}

# --- Auto-generated (do not set manually) ---
# The CI pipeline (ci-setup) creates these automatically on first run.
# One per service in services.txt, e.g.:
#   QUIZZER_POSTGRES_URL=postgresql://quizzer_user:pass@postgres.railway.internal:5432/quizzer_db
#   AUTH_POSTGRES_URL=postgresql://auth_user:pass@postgres.railway.internal:5432/auth_db

# --- Optional ---

# Path to the services file inside the container.
# Default: services.txt
#SERVICES_FILE=services.txt


# --- Railway service variables (set in Railway dashboard, not CI) ---
#
# POSTGRES_URL — superuser connection URL for the provisioner binary.
#   Set as a Railway reference, e.g. ${{Postgres-18.DATABASE_URL}}
#   Only the provisioner uses this; ci-setup does not read it.
